def get_employee_id(full_name: str) -> int:
    """Get the employee id of a person.
    
    Args:
        full_name: The full name of the person with first and last name
        
    Returns:
        The employee ID as an integer
    """
    print(f"GetEmployeeId function called with parameter: fullName = {full_name}")
    return 42
